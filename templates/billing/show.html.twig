{% extends 'base.html.twig' %}

{% block title %}Facture{% endblock %}

{% block body %}
    <h1>Facture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ billing.id }}</td>
            </tr>
            <tr>
                <th>Contract_id</th>
                <td>{{ billing.ContractId }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ billing.Amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_billing_edit', {'id': billing.id}) }}">Editer</a>

    {{ include('billing/_delete_form.html.twig') }}    
    
    <a href="{{ path('app_billing_index') }}">Retour à la liste</a>
{% endblock %}
