<form method="post" action="{{ path('vehicle_delete', {'id': vehicle._id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vehicle._id) }}">
    <button class="btn">Delete</button>
</form>